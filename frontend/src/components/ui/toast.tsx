/**
 * Re-export sonner's toast API for use throughout the app.
 * Usage: import { toast } from '@/components/ui/toast'
 *        toast.success('保存しました')
 *        toast.error('エラーが発生しました')
 */
export { toast } from 'sonner';
